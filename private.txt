```plaintext
----- FICHIER : package.json (à la racine) -----
{
  "name": "bianca-costa-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build"
  },
  "dependencies": {
    "framer-motion": "^7.6.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.11.2",
    "react-scripts": "5.0.1"
  }
}

----- FICHIER : public/index.html -----
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, maximum-scale=1"
    />
    <link rel="icon" href="%PUBLIC_URL%/favicon.ico" />
    <title>Bianca Costa - Official Website</title>
  </head>
  <body>
    <noscript>You need to enable JavaScript to run this app.</noscript>
    <div id="root"></div>
  </body>
</html>

----- FICHIER : src/index.js -----
import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import App from "./App";
import "./index.css";
import { LanguageProvider } from "./context/LanguageContext";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <LanguageProvider>
      <BrowserRouter>
        <App />
      </BrowserRouter>
    </LanguageProvider>
  </React.StrictMode>
);

----- FICHIER : src/index.css -----
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: "Poppins", Arial, sans-serif;
  background-color: #fafafa;
  color: #333;
}

.container {
  width: 90%;
  max-width: 1200px;
  margin: 0 auto;
}

----- FICHIER : src/App.js -----
import React from "react";
import { Routes, Route } from "react-router-dom";
import Header from "./components/Header/Header";
import Footer from "./components/Footer/Footer";
import HomePage from "./pages/HomePage";
import "./App.css";

function App() {
  return (
    <>
      <Header />
      <Routes>
        <Route path="/" element={<HomePage />} />
      </Routes>
      <Footer />
    </>
  );
}

export default App;

----- FICHIER : src/App.css -----
html,
body {
  scroll-behavior: smooth;
}

----- FICHIER : src/context/LanguageContext.js -----
import React, { createContext, useState, useContext } from "react";

const LanguageContext = createContext();

const translations = {
  fr: {
    header: {
      home: "Accueil",
      music: "Musique",
      videos: "Vidéos",
      concerts: "Concerts",
      gallery: "Galerie",
      shop: "Shop",
      contact: "Contact"
    },
    hero: {
      title: "Bianca Costa",
      subtitle: "Bossa Nova & Trap - L'univers Franco-Brésilien"
    },
    music: {
      title: "Ma Musique",
      description: "Écoutez mes morceaux sur Spotify"
    },
    videos: {
      title: "Vidéos"
    },
    concerts: {
      title: "Concerts à venir",
      button: "Acheter un billet"
    },
    gallery: {
      title: "Galerie",
      filterAll: "Tous",
      filterPhotos: "Photos",
      filterVideos: "Vidéos"
    },
    shop: {
      title: "Boutique",
      buy: "Acheter"
    },
    contact: {
      title: "Contact",
      name: "Nom",
      email: "Email",
      message: "Message",
      submit: "Envoyer"
    },
    footer: {
      newsletter: "Abonnez-vous à ma newsletter",
      subscribe: "S'abonner"
    }
  },
  en: {
    header: {
      home: "Home",
      music: "Music",
      videos: "Videos",
      concerts: "Concerts",
      gallery: "Gallery",
      shop: "Shop",
      contact: "Contact"
    },
    hero: {
      title: "Bianca Costa",
      subtitle: "Bossa Nova & Trap - Franco-Brazilian Universe"
    },
    music: {
      title: "My Music",
      description: "Listen to my tracks on Spotify"
    },
    videos: {
      title: "Videos"
    },
    concerts: {
      title: "Upcoming Concerts",
      button: "Buy Ticket"
    },
    gallery: {
      title: "Gallery",
      filterAll: "All",
      filterPhotos: "Photos",
      filterVideos: "Videos"
    },
    shop: {
      title: "Shop",
      buy: "Buy"
    },
    contact: {
      title: "Contact",
      name: "Name",
      email: "Email",
      message: "Message",
      submit: "Send"
    },
    footer: {
      newsletter: "Subscribe to my newsletter",
      subscribe: "Subscribe"
    }
  },
  pt: {
    header: {
      home: "Início",
      music: "Música",
      videos: "Vídeos",
      concerts: "Shows",
      gallery: "Galeria",
      shop: "Loja",
      contact: "Contato"
    },
    hero: {
      title: "Bianca Costa",
      subtitle: "Bossa Nova & Trap - Universo Franco-Brasileiro"
    },
    music: {
      title: "Minha Música",
      description: "Ouça minhas faixas no Spotify"
    },
    videos: {
      title: "Vídeos"
    },
    concerts: {
      title: "Próximos Shows",
      button: "Comprar Ingresso"
    },
    gallery: {
      title: "Galeria",
      filterAll: "Todos",
      filterPhotos: "Fotos",
      filterVideos: "Vídeos"
    },
    shop: {
      title: "Loja",
      buy: "Comprar"
    },
    contact: {
      title: "Contato",
      name: "Nome",
      email: "Email",
      message: "Mensagem",
      submit: "Enviar"
    },
    footer: {
      newsletter: "Inscreva-se na minha newsletter",
      subscribe: "Inscrever-se"
    }
  }
};

export const LanguageProvider = ({ children }) => {
  const [language, setLanguage] = useState("fr");
  const switchLanguage = (lang) => {
    setLanguage(lang);
  };
  return (
    <LanguageContext.Provider value={{ language, switchLanguage, t: translations[language] }}>
      {children}
    </LanguageContext.Provider>
  );
};

export const useLanguage = () => useContext(LanguageContext);

----- FICHIER : src/pages/HomePage.js -----
import React from "react";
import Hero from "../components/Hero/Hero";
import Music from "../components/Music/Music";
import Videos from "../components/Videos/Videos";
import Concerts from "../components/Concerts/Concerts";
import Gallery from "../components/Gallery/Gallery";
import Shop from "../components/Shop/Shop";
import Contact from "../components/Contact/Contact";
import Social from "../components/Social/Social";

function HomePage() {
  return (
    <div>
      <Hero />
      <Music />
      <Videos />
      <Concerts />
      <Gallery />
      <Shop />
      <Contact />
      <Social />
    </div>
  );
}

export default HomePage;

----- FICHIER : src/components/Header/Header.js -----
import React, { useState } from "react";
import "./Header.css";
import { Link } from "react-router-dom";
import { useLanguage } from "../../context/LanguageContext";

function Header() {
  const { language, switchLanguage, t } = useLanguage();
  const [isOpen, setIsOpen] = useState(false);

  const handleBurgerClick = () => {
    setIsOpen(!isOpen);
  };

  return (
    <header className="header-container">
      <div className="logo">
        <Link to="/">{t.hero.title}</Link>
      </div>
      <nav className={`nav-links ${isOpen ? "open" : ""}`}>
        <a href="#hero" onClick={() => setIsOpen(false)}>
          {t.header.home}
        </a>
        <a href="#music" onClick={() => setIsOpen(false)}>
          {t.header.music}
        </a>
        <a href="#videos" onClick={() => setIsOpen(false)}>
          {t.header.videos}
        </a>
        <a href="#concerts" onClick={() => setIsOpen(false)}>
          {t.header.concerts}
        </a>
        <a href="#gallery" onClick={() => setIsOpen(false)}>
          {t.header.gallery}
        </a>
        <a href="#shop" onClick={() => setIsOpen(false)}>
          {t.header.shop}
        </a>
        <a href="#contact" onClick={() => setIsOpen(false)}>
          {t.header.contact}
        </a>
      </nav>
      <div className="language-switch">
        <button
          className={language === "fr" ? "active" : ""}
          onClick={() => switchLanguage("fr")}
        >
          FR
        </button>
        <button
          className={language === "en" ? "active" : ""}
          onClick={() => switchLanguage("en")}
        >
          EN
        </button>
        <button
          className={language === "pt" ? "active" : ""}
          onClick={() => switchLanguage("pt")}
        >
          PT
        </button>
      </div>
      <div className="burger" onClick={handleBurgerClick}>
        <span />
        <span />
        <span />
      </div>
    </header>
  );
}

export default Header;

----- FICHIER : src/components/Header/Header.css -----
.header-container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: #ffcc00;
  padding: 1rem;
  position: sticky;
  top: 0;
  z-index: 999;
}

.logo a {
  font-weight: bold;
  font-size: 1.2rem;
  text-decoration: none;
  color: #333;
}

.nav-links {
  display: flex;
  gap: 1rem;
  transition: transform 0.3s ease;
}

.nav-links a {
  text-decoration: none;
  color: #333;
  font-weight: 500;
}

.language-switch button {
  margin: 0 5px;
  border: none;
  background: transparent;
  cursor: pointer;
  font-weight: bold;
}

.language-switch .active {
  text-decoration: underline;
}

.burger {
  width: 30px;
  height: 20px;
  display: none;
  flex-direction: column;
  justify-content: space-between;
  cursor: pointer;
}

.burger span {
  display: block;
  height: 3px;
  background: #333;
  border-radius: 2px;
}

@media (max-width: 768px) {
  .nav-links {
    position: absolute;
    top: 70px;
    right: 0;
    background: #ffcc00;
    flex-direction: column;
    width: 200px;
    transform: translateX(100%);
  }
  .nav-links.open {
    transform: translateX(0);
  }
  .burger {
    display: flex;
  }
}

----- FICHIER : src/components/Hero/Hero.js -----
import React from "react";
import "./Hero.css";
import { useLanguage } from "../../context/LanguageContext";
import { motion } from "framer-motion";

function Hero() {
  const { t } = useLanguage();
  return (
    <section id="hero" className="hero-container">
      <motion.div
        className="hero-content"
        initial={{ opacity: 0, y: 50 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
        transition={{ duration: 1 }}
      >
        <h1>{t.hero.title}</h1>
        <p>{t.hero.subtitle}</p>
      </motion.div>
      <motion.div
        className="hero-slides"
        initial={{ opacity: 0, scale: 0.8 }}
        whileInView={{ opacity: 1, scale: 1 }}
        viewport={{ once: true }}
        transition={{ duration: 1 }}
      >
        <img src="/bibi.png" alt="Bianca Costa" />
      </motion.div>
    </section>
  );
}

export default Hero;

----- FICHIER : src/components/Hero/Hero.css -----
.hero-container {
  width: 100%;
  height: 80vh;
  background: linear-gradient(to right, #ffa500, #ff5e00);
  color: #fff;
  display: flex;
  align-items: center;
  justify-content: space-around;
  flex-wrap: wrap;
  position: relative;
}

.hero-content {
  max-width: 600px;
  margin: 1rem;
}

.hero-content h1 {
  font-size: 3rem;
  margin-bottom: 1rem;
}

.hero-content p {
  font-size: 1.5rem;
}

.hero-slides img {
  width: 300px;
  object-fit: cover;
  border-radius: 10px;
}

----- FICHIER : src/components/Music/Music.js -----
import React from "react";
import "./Music.css";
import { useLanguage } from "../../context/LanguageContext";
import { motion } from "framer-motion";

function Music() {
  const { t } = useLanguage();
  return (
    <motion.section
      id="music"
      className="music-section container"
      initial={{ opacity: 0, y: 50 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true }}
      transition={{ duration: 1 }}
    >
      <h2>{t.music.title}</h2>
      <p>{t.music.description}</p>
      <div className="spotify-player">
        <iframe
          src="https://open.spotify.com/embed/playlist/37i9dQZF1DX1ClZR1U5X5X"
          width="100%"
          height="380"
          frameBorder="0"
          allow="encrypted-media"
          title="Spotify Player"
        />
      </div>
    </motion.section>
  );
}

export default Music;

----- FICHIER : src/components/Music/Music.css -----
.music-section {
  text-align: center;
  margin: 2rem 0;
}

.music-section h2 {
  font-size: 2rem;
  margin-bottom: 1rem;
}

.spotify-player {
  max-width: 600px;
  margin: 0 auto;
}

----- FICHIER : src/components/Videos/Videos.js -----
import React from "react";
import "./Videos.css";
import { useLanguage } from "../../context/LanguageContext";
import { motion } from "framer-motion";

function Videos() {
  const { t } = useLanguage();
  return (
    <motion.section
      id="videos"
      className="videos-section container"
      initial={{ opacity: 0, y: 50 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true }}
      transition={{ duration: 1 }}
    >
      <h2>{t.videos.title}</h2>
      <div className="video-wrapper">
        <iframe
          width="560"
          height="315"
          src="https://www.youtube.com/embed/YOUR_VIDEO_ID"
          title="YouTube Video"
          frameBorder="0"
          allowFullScreen
        />
      </div>
    </motion.section>
  );
}

export default Videos;

----- FICHIER : src/components/Videos/Videos.css -----
.videos-section {
  margin: 2rem 0;
  text-align: center;
}

.video-wrapper {
  display: flex;
  flex-wrap: wrap;
  gap: 2rem;
  justify-content: center;
}

.video-wrapper iframe {
  width: 560px;
  max-width: 100%;
  height: 315px;
  border: none;
}

----- FICHIER : src/components/Concerts/Concerts.js -----
import React from "react";
import "./Concerts.css";
import { useLanguage } from "../../context/LanguageContext";
import { motion } from "framer-motion";

function Concerts() {
  const { t } = useLanguage();
  const concertsData = [
    { date: "2025-03-15", city: "Paris", venue: "Le Zénith" },
    { date: "2025-04-10", city: "Rio de Janeiro", venue: "Vivo Rio" }
  ];
  return (
    <motion.section
      id="concerts"
      className="concerts-section container"
      initial={{ opacity: 0, y: 50 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true }}
      transition={{ duration: 1 }}
    >
      <h2>{t.concerts.title}</h2>
      <ul className="concert-list">
        {concertsData.map((concert, index) => (
          <li key={index} className="concert-item">
            <span className="concert-date">{concert.date}</span>
            <span className="concert-city">{concert.city}</span>
            <span className="concert-venue">{concert.venue}</span>
            <button className="ticket-btn">{t.concerts.button}</button>
          </li>
        ))}
      </ul>
    </motion.section>
  );
}

export default Concerts;

----- FICHIER : src/components/Concerts/Concerts.css -----
.concerts-section {
  margin: 2rem 0;
  text-align: center;
}

.concert-list {
  list-style: none;
  padding: 0;
  margin: 1rem auto;
  max-width: 500px;
}

.concert-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 1rem;
  border: 1px solid #ccc;
  padding: 1rem;
  border-radius: 5px;
}

.concert-item span {
  margin: 0.3rem 0;
}

.ticket-btn {
  margin-top: 0.5rem;
  padding: 0.5rem 1rem;
  border: none;
  background-color: #ff5e00;
  color: #fff;
  cursor: pointer;
  border-radius: 5px;
}

----- FICHIER : src/components/Gallery/Gallery.js -----
import React, { useState } from "react";
import "./Gallery.css";
import { useLanguage } from "../../context/LanguageContext";
import { motion } from "framer-motion";

const data = [
  { id: 1, type: "photo", src: "/bibi.png", alt: "Photo 1" },
  { id: 2, type: "photo", src: "/bibi2.png", alt: "Photo 2" },
  { id: 3, type: "video", src: "https://www.youtube.com/embed/YOUR_VIDEO_ID", alt: "Video" }
];

function Gallery() {
  const { t } = useLanguage();
  const [filter, setFilter] = useState("all");
  const handleFilter = (type) => {
    setFilter(type);
  };
  const filteredData =
    filter === "all" ? data : data.filter((item) => item.type === filter);

  return (
    <motion.section
      id="gallery"
      className="gallery-section container"
      initial={{ opacity: 0, y: 50 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true }}
      transition={{ duration: 1 }}
    >
      <h2>{t.gallery.title}</h2>
      <div className="gallery-filters">
        <button onClick={() => handleFilter("all")}>{t.gallery.filterAll}</button>
        <button onClick={() => handleFilter("photo")}>{t.gallery.filterPhotos}</button>
        <button onClick={() => handleFilter("video")}>{t.gallery.filterVideos}</button>
      </div>
      <div className="gallery-grid">
        {filteredData.map((item) =>
          item.type === "photo" ? (
            <img key={item.id} src={item.src} alt={item.alt} />
          ) : (
            <div key={item.id} className="video-wrapper">
              <iframe
                width="100%"
                height="200"
                src={item.src}
                title="Video"
                frameBorder="0"
                allowFullScreen
              />
            </div>
          )
        )}
      </div>
    </motion.section>
  );
}

export default Gallery;

----- FICHIER : src/components/Gallery/Gallery.css -----
.gallery-section {
  text-align: center;
  margin: 2rem 0;
}

.gallery-filters {
  margin-bottom: 1rem;
}

.gallery-filters button {
  margin: 0 0.5rem;
  padding: 0.5rem 1rem;
  border: none;
  background-color: #aaa;
  color: #fff;
  cursor: pointer;
  border-radius: 5px;
}

.gallery-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
  gap: 1rem;
}

.gallery-grid img {
  width: 100%;
  height: auto;
  object-fit: cover;
  border-radius: 5px;
}

.video-wrapper {
  position: relative;
  padding-bottom: 56.25%;
  height: 0;
}

.video-wrapper iframe {
  position: absolute;
  width: 100%;
  height: 100%;
  left: 0;
  top: 0;
}

----- FICHIER : src/components/Shop/Shop.js -----
import React from "react";
import "./Shop.css";
import { useLanguage } from "../../context/LanguageContext";
import { motion } from "framer-motion";

function Shop() {
  const { t } = useLanguage();
  const products = [
    { id: 1, name: "Album CD", price: 15, img: "/logo192.png" },
    { id: 2, name: "T-Shirt", price: 25, img: "/logo512.png" }
  ];
  return (
    <motion.section
      id="shop"
      className="shop-section container"
      initial={{ opacity: 0, y: 50 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true }}
      transition={{ duration: 1 }}
    >
      <h2>{t.shop.title}</h2>
      <div className="shop-grid">
        {products.map((prod) => (
          <div key={prod.id} className="shop-item">
            <img src={prod.img} alt={prod.name} />
            <h3>{prod.name}</h3>
            <p>${prod.price}</p>
            <button>{t.shop.buy}</button>
          </div>
        ))}
      </div>
    </motion.section>
  );
}

export default Shop;

----- FICHIER : src/components/Shop/Shop.css -----
.shop-section {
  margin: 2rem 0;
  text-align: center;
}

.shop-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  justify-content: center;
}

.shop-item {
  border: 1px solid #ccc;
  border-radius: 5px;
  padding: 1rem;
  width: 200px;
}

.shop-item img {
  max-width: 100%;
  height: auto;
}

----- FICHIER : src/components/Contact/Contact.js -----
import React, { useState } from "react";
import "./Contact.css";
import { useLanguage } from "../../context/LanguageContext";
import { motion } from "framer-motion";

function Contact() {
  const { t } = useLanguage();
  const [formData, setFormData] = useState({ name: "", email: "", message: "" });

  const handleChange = (e) => {
    setFormData((prev) => ({ ...prev, [e.target.name]: e.target.value }));
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    console.log("Form submitted:", formData);
    alert("Message envoyé !");
    setFormData({ name: "", email: "", message: "" });
  };

  return (
    <motion.section
      id="contact"
      className="contact-section container"
      initial={{ opacity: 0, y: 50 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true }}
      transition={{ duration: 1 }}
    >
      <h2>{t.contact.title}</h2>
      <form onSubmit={handleSubmit} className="contact-form">
        <label>
          {t.contact.name}
          <input
            type="text"
            name="name"
            value={formData.name}
            onChange={handleChange}
            required
          />
        </label>
        <label>
          {t.contact.email}
          <input
            type="email"
            name="email"
            value={formData.email}
            onChange={handleChange}
            required
          />
        </label>
        <label>
          {t.contact.message}
          <textarea
            name="message"
            rows="5"
            value={formData.message}
            onChange={handleChange}
            required
          />
        </label>
        <button type="submit">{t.contact.submit}</button>
      </form>
    </motion.section>
  );
}

export default Contact;

----- FICHIER : src/components/Contact/Contact.css -----
.contact-section {
  margin: 2rem 0;
  text-align: center;
}

.contact-form {
  display: flex;
  flex-direction: column;
  margin: 1rem auto;
  max-width: 400px;
}

.contact-form label {
  margin-bottom: 1rem;
  text-align: left;
}

.contact-form input,
.contact-form textarea {
  width: 100%;
  padding: 0.5rem;
  margin-top: 0.5rem;
  border-radius: 4px;
  border: 1px solid #ccc;
}

.contact-form button {
  padding: 0.5rem 1rem;
  border: none;
  background-color: #ff5e00;
  color: #fff;
  cursor: pointer;
  border-radius: 5px;
}

----- FICHIER : src/components/Social/Social.js -----
import React from "react";
import "./Social.css";

function Social() {
  return (
    <section className="social-section container">
      <h3>Follow Me</h3>
      <div className="social-links">
        <a
          href="https://www.instagram.com/biancacostaofficial"
          target="_blank"
          rel="noreferrer"
        >
          Instagram
        </a>
        <a
          href="https://twitter.com/biancacosta"
          target="_blank"
          rel="noreferrer"
        >
          Twitter
        </a>
        <a
          href="https://www.youtube.com/biancacosta"
          target="_blank"
          rel="noreferrer"
        >
          YouTube
        </a>
      </div>
    </section>
  );
}

export default Social;

----- FICHIER : src/components/Social/Social.css -----
.social-section {
  margin: 2rem 0;
  text-align: center;
}

.social-links {
  display: flex;
  justify-content: center;
  gap: 1rem;
}

.social-links a {
  text-decoration: none;
  color: #333;
  font-weight: bold;
}

----- FICHIER : src/components/Footer/Footer.js -----
import React, { useState } from "react";
import "./Footer.css";
import { useLanguage } from "../../context/LanguageContext";

function Footer() {
  const { t } = useLanguage();
  const [email, setEmail] = useState("");

  const handleNewsletter = (e) => {
    e.preventDefault();
    console.log("Newsletter subscription:", email);
    alert("Merci pour votre inscription !");
    setEmail("");
  };

  return (
    <footer className="footer-container">
      <form onSubmit={handleNewsletter} className="newsletter-form">
        <label>{t.footer.newsletter}</label>
        <div className="newsletter-input">
          <input
            type="email"
            placeholder="Votre email"
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            required
          />
          <button type="submit">{t.footer.subscribe}</button>
        </div>
      </form>
      <p>© 2025 Bianca Costa - All rights reserved.</p>
    </footer>
  );
}

export default Footer;

----- FICHIER : src/components/Footer/Footer.css -----
.footer-container {
  background: #333;
  color: #fff;
  text-align: center;
  padding: 2rem 1rem;
}

.newsletter-form {
  margin-bottom: 1rem;
}

.newsletter-form label {
  display: block;
  margin-bottom: 0.5rem;
  font-weight: bold;
}

.newsletter-input {
  display: flex;
  justify-content: center;
  max-width: 400px;
  margin: 0 auto;
}

.newsletter-input input {
  flex: 1;
  padding: 0.5rem;
  border: none;
  border-radius: 5px 0 0 5px;
}

.newsletter-input button {
  padding: 0.5rem 1rem;
  background-color: #ff5e00;
  border: none;
  border-radius: 0 5px 5px 0;
  color: #fff;
  cursor: pointer;
}
```